steps:
  - label: "AMDGPU - Julia 1.12"
    plugins:
      - JuliaCI/julia#v1:
          version: "1.12"
      - JuliaCI/julia-test#v1:
      - JuliaCI/julia-coverage#v1:
          codecov: true
    agents:
      queue: "rocm"
      rocmgpu: "*"
    if: build.message !~ /\[skip tests\]/
    command: "julia --project -e 'using Pkg; Pkg.update()'"
    timeout_in_minutes: 20
    env:
      JULIA_NUM_THREADS: 4
      NNKERN_TEST_AMDGPU: true

  - label: "CUDA - Julia 1.12"
    plugins:
      - JuliaCI/julia#v1:
          version: "1.12"
      - JuliaCI/julia-test#v1:
      - JuliaCI/julia-coverage#v1:
          codecov: true
    agents:
      queue: "cuda"
    if: build.message !~ /\[skip tests\]/
    command: "julia --project -e 'using Pkg; Pkg.update()'"
    timeout_in_minutes: 20
    env:
      JULIA_NUM_THREADS: 4
      NNKERN_TEST_CUDA: true
